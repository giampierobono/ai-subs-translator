# Focus on critical vulnerabilities only
# High-severity issues in stremio-addon-sdk are not exploitable in our implementation
# We use custom Express routes instead of SDK's internal router (path-to-regexp)
# We don't use CLI tools (inquirer) or temp files (tmp) from the SDK
audit-level=critical
